# Clone the repository
git clone https://github.com/MdZahidulIslam-Bismi/family-web.git

# Move into the project folder
cd family-web

# Install dependencies
npm install

# If you face a Tailwind or Vite plugin issue, run:
npm install -D tailwindcss @tailwindcss/vite

# Start the development server
npm run dev
